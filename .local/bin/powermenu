#! /bin/sh

CHOSEN=$(printf "Power Off\nRestart\nLock\nLogout" | rofi -p "Powermenu" -dmenu)

case $CHOSEN in
  "Power Off") systemctl poweroff ;;
  "Restart") systemctl reboot ;;
  "Lock") betterlockscreen -l;;
  "Logout") loginctl terminate-user $USER ;;
  *) exit 1 ;;
esac
